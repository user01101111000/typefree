const settings_menu_list = {
    info: {
        namespace: "A namespace in TypeScript is a way to group related code together and avoid name conflicts. It provides a container for interfaces, types, and functions, making the code more modular and organized. However, modern projects often use ES modules (export / import) instead of namespaces.",
        prefix: "In TypeScript, a prefix is commonly used in naming conventions to indicate scope, type, or purpose, such as _privateVar, IInterfaceName, or TTypeAlias.",
        autoSave: "If auto save is active, the last code you wrote will remain in the editor even if you exit the site."
    }
};

export default settings_menu_list;